/**
 * Created by psmaster on 13/3/2015.
 */
/*angular.module('app')
 .service('socket', ['$localStorage',function ($localStorage) {
 'use strict';

        var state = {};
        var exampleSocket = new WebSocket("ws://localhost:9997/events");

        exampleSocket.onmessage = function (event) {
            if(angular.isDefined($localStorage.authed)){
                state = event;
            }
        };

 return {
 state: state
 };
 }]);*/
